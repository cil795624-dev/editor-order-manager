# 图编单口排单管家

这是给 GitHub Pages 使用的公开静态网页版本。

## 使用方式

1. 新建一个公开 GitHub 仓库。
2. 把本文件夹里的所有文件上传到仓库根目录。
3. 在仓库 Settings -> Pages 里选择 `Deploy from a branch`。
4. Branch 选择 `main`，文件夹选择 `/root`，保存。
5. 等 GitHub Pages 生成网址后，用 Safari 打开并添加到主屏幕。

数据只保存在每个使用者自己的浏览器本机，不会和其他人互通。
